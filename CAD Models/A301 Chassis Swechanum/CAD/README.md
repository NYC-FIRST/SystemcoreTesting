# Swechanum CAD Source Status

The final editable Swechanum Fusion 360 and STEP files are not stored in this public repository because their file sizes exceed the normal GitHub limit and this public fork cannot accept new Git LFS objects.

The repository still includes the chassis documentation, physical-build photos, and reference CAD render in the parent folder.
